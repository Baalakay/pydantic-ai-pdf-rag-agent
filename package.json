{
  "name": "pdf-rag-agent",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "backend:dev": "cd backend && uvicorn src.app.main:app --reload",
    "backend:install": "cd backend && pip install -r requirements.txt",
    "backend:test": "cd backend && pytest",
    "backend:lint": "cd backend && mypy src tests"
  },
  "dependencies": {
    "@radix-ui/react-slot": "^1.1.1",
    "clsx": "^2.1.1",
    "tailwind-merge": "^2.6.0"
  }
}
